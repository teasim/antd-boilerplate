import React from "react";
import { Button, Icon } from "teasim";

const LaunchButton = () => (
  <Button 
    outline
    quadrate
    size="medium" 
    skin="default">
    <Icon name="nine-point-filled" />
  </Button>
)

export default LaunchButton;
