// import { fromJS } from 'immutable';
// import { makeSelectFormBDomain } from '../selectors';

// const selector = makeSelectFormBDomain();

describe('makeSelectFormBDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
