// import { fromJS } from 'immutable';
// import { makeSelectFormADomain } from '../selectors';

// const selector = makeSelectFormADomain();

describe('makeSelectFormADomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
